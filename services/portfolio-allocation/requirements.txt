-e ../../shared
fastapi>=0.115
uvicorn[standard]>=0.34
pydantic>=2.9
torch>=2.2
pandas>=2.2
numpy>=1.26
scikit-learn>=1.4
joblib>=1.3
requests>=2.31
python-dotenv>=1.0

# Minimum-version ranges, not exact pins -- see services/car-magnitude/requirements.txt
# for why. inference.py loads a PyTorch checkpoint (app/data/models/final_model.pt),
# not Keras -- no tensorflow needed. The RL model code under app/rl/ needs only
# torch + numpy + pandas + scikit-learn (no torch_geometric, matplotlib, or
# tensorboard -- those are only used by the original training/plotting code,
# not by inference).
