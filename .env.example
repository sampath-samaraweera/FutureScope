# Shared across every service under services/ (car-magnitude, return-forecast,
# portfolio-allocation, ...). Copy this file to .env in this same directory
# and fill it in -- each service loads it via cse_lib.env.load_shared_env().
#
# See services/car-magnitude/.env.example for how to obtain this token.
CSE_ACCESS_TOKEN=
